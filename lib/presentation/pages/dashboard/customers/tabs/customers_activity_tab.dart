import 'package:flutter/material.dart';

class CustomersActivityTab extends StatelessWidget {
  const CustomersActivityTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
